module CSS.StylesheetForCompilation (
    module CSS.Stylesheet
  ) where

import Prelude


-- import Control.Apply ((*>))
-- import Data.NonEmpty    as NEL

import CSS.Stylesheet



-- -- | For chaining together multiple ?'s in MediaQueries
-- chain :: Array CSS -> CSS
-- chain [] = S unit
-- chain xs = fold
